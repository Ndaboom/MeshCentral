/**
 * MeshCentral UI component catalog.
 *
 * Runtime component files live under `public/js/components/`:
 * - `core/dom-utils.js` exposes shared DOM helpers for components.
 * - `foundation/modern-modal.js` exposes ModernModal/createModernModal.
 * - `foundation/modern-card.js` exposes ModernCard/createModernCard.
 * - `modals/open-modal.js` exposes openModal.
 * - `forms/input-modal.js` exposes openInputModal.
 * - `forms/textarea-modal.js` exposes openTextareaModal.
 * - `forms/select-modal.js` exposes openSelectModal.
 * - `modals/confirm-modal.js` exposes openConfirmModal.
 * - `display/copyable-value.js` exposes copyableValue.
 * - `uploads/icon-upload.js` exposes IconUploadComponent/createIconUploadComponent.
 * - `uploads/upload-modal.js` exposes openUploadModal.
 *
 * Keep page templates loading the concrete component files they need. This file
 * intentionally has no runtime behavior so component ownership stays clear.
 */
